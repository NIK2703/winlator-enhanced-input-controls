// entry.c — all functions migrated to gesture_branch / base.c / handler.c.
// This file is excluded from the build (see CMakeLists.txt).
// The old enter_sdtw(time_ms) signature is kept for reference; the new
// signature is enter_sdtw(TouchFinger*, uint64_t) in touch_processor_internal.h.

